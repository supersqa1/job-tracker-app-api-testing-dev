name: Demo 1 - Just Dummy single job

on: 
  workflow_dispatch:

jobs:
    my-job-1:
        name: My first dummy job
        runs-on: ubuntu-latest
        steps:
          - name: my first step
            uses: actions/checkout@v5

          - name: my second step
            run: echo "This is my first print message"
    my-job-2:
        name: My first dummy job
        runs-on: ubuntu-latest
        steps:
            - name: my first step
              uses: actions/checkout@v5

            - name: my second step
              run: echo "This is my first print message"