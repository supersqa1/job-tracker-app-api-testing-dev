name: Demo 01 - API Tests Echo

on:
  workflow_dispatch:

jobs:
  api-tests:
    name: Simulate API Test Job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Show API test job purpose
        run: echo "This job represents running our API test automation suite."

      - name: Install dependencies simulation
        run: echo "Installing Python dependencies from requirements.txt..."

      - name: Run API tests simulation
        run: echo "Running pytest for API tests..."

      - name: Publish result simulation
        run: echo "API test job completed successfully."
